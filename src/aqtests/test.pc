/*

fib = (fib,x) -> {
  if (x < 2) {
  	return 1;
  }
  return fib(x-1)*fib(x-2);
};
y = fib(fib, 9); // same as fib.fib(9)


myList = [1,2,3];

(myList.where)();

ACCESS -> 
	ID myList
	where




myFunc = (a, b) -> {
	return a[1:b];
};
result = myFunc("hey dude", 2);
print(result);

o = {
	a: () -> { return 7; },
	b: 9,
	c: "hey",
	d: (x) -> { return { e: x }; }
};

myFunc = (obj) -> { return obj.e; };

r = myFunc(o.d(o.c));

*/

print("hey");

x = () -> { print("hey"); return 4; };




