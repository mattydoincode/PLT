
identity = x -> { return x; };

myNums = List.map([1,2,3], identity);

unwrap = (o) -> { return List.map("hey", o.a.b[0]); };

obj = {
    i: identity,
    wrap: (x) -> { return { a: { b: [x] } }; }
};

myString = unwrap(obj.wrap(obj.i));

myFunc = (f,a) -> { return f(a); };

aNumber = myFunc(a -> { return a+1; }, 5);
aString = myFunc(a -> { return a^"\ndude"; }, "what's up");

print(myString);
print(aNumber);
print(aString);
